spring:
  application:
    name: atlas-scraper

server:
  port: ${SCRAPER_PORT:8081}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
