apiVersion: v1
kind: Service
metadata:
  name: atlas-scraper-service
  namespace: atlas-events
  labels:
    app: atlas-scraper
spec:
  selector:
    app: atlas-scraper
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
  type: ClusterIP
