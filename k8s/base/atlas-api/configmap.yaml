apiVersion: v1
kind: ConfigMap
metadata:
  name: atlas-api-config
  namespace: atlas-events
data:
  SPRING_PROFILES_ACTIVE: "k8s"
  APP_PORT: "8080"
  RABBITMQ_HOST: "rabbitmq-service"
  RABBITMQ_PORT: "5672"
  CORS_ALLOWED_ORIGINS: "https://atlas-events.ma"
