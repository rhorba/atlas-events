apiVersion: v1
kind: Service
metadata:
  name: atlas-api
  namespace: atlas-events
  labels:
    app: atlas-api
spec:
  selector:
    app: atlas-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
