apiVersion: v1
kind: Service
metadata:
  name: atlas-web-service
  namespace: atlas-events
  labels:
    app: atlas-web
spec:
  selector:
    app: atlas-web
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
