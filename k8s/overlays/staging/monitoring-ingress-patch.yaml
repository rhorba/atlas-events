apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: monitoring-ingress
  namespace: monitoring
spec:
  tls:
    - hosts:
        - grafana.staging.atlas-events.ma
      secretName: grafana-staging-tls
  rules:
    - host: grafana.staging.atlas-events.ma
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana-service
                port:
                  number: 3000
